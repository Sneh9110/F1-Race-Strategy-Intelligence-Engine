global:
  resolve_timeout: 5m

route:
  receiver: "default"
  group_by: ["alertname", "service"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: "default"
    # Configure Slack/PagerDuty/email here via env-specific overlays
    # slack_configs:
    #   - api_url: ${SLACK_WEBHOOK_URL}
    #     channel: "#alerts"
    #     title: "{{ .CommonLabels.alertname }}"
    #     text: "{{ .CommonAnnotations.description }}"
